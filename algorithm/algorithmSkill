回溯 back tracking
回溯法是一种选优搜索法，又称为试探法，按选优条件向前搜索，以达到目标。但当探索到某一步时，发现原先选择并不优或达不到目标，
就退回一步重新选择，这种走不通就退回再走的技术为回溯法，而满足回溯条件的某个状态的点称为“回溯点”
题目 lc22 lc51 lc52 N皇后问题

动态规划和记忆化搜索的关系：
一般而言：动态规划是一种算法，有两种实现方式：递推和记忆化搜索，但是我在解释的过程中，
直接将递推解释成动态规划
有的问题使用动态规划比较容易，有的问题使用记忆化搜素比较容易。但是只要是动态规划问题，使用递推和记忆化搜索都可以解，
存在的问题只是好不好解的问题。

动态规划：递推，自上而下，即先算小规模问题，再算大规模的问题
记忆化搜索：自下而上，即直接开始计算大规模问题，大规模问题由小规模问题组成，
          于是将小规模问题的解记录下来，等到再次需要该小规模问题的解时，不再需要重复计算。
          这利用了朴素回溯的形式，但是回溯是不记录小规模问题的解的，因此会出现大量的重复计算。
          所以，可以使用记忆化搜索解的问题，必然可以使用朴素回溯。
题目：lc63 lc64 lc65 这三道题可以使用动态规划，也可以使用记忆化搜索，当然也可以使用朴素回溯。


